{
  "name": "shift-alert-billing-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:apply": "wrangler d1 migrations apply canada-shift-watcher-db",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^3.91.0"
  }
}
